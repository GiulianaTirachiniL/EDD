/* #include <stdio.h>
#include "utils.h"

void Enter(int magezone_id, int magemite_id, int atk, int speed) {
    Magemite *new_magemite = calloc(1, sizeof(Magemite));
    new_magemite->magemite_id = magemite_id;
    new_magemite->atk = atk;    
    new_magemite->speed = speed;

    // new_magemite->next = ;
} */